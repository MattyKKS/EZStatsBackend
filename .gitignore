# dependencies
/node_modules

# build
/dist
*.tsbuildinfo

# env
.env
.env.local

# prisma
/prisma/migrations/dev.db*

# logs
*.log
npm-debug.log*

# editor / os
.DS_Store
.idea
.vscode/*
!.vscode/extensions.json

# local AI-worker outputs mounted for ingestion (never commit run data)
/worker_outputs

# uploaded files (team logos, etc.) — keep the folder, ignore its contents
/uploads/*
!/uploads/.gitkeep
